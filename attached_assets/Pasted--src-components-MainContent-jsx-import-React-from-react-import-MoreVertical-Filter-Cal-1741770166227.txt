// src/components/MainContent.jsx
import React from "react";
import {
  MoreVertical, Filter, Calendar, Columns, Download, Search,
  Plus, Repeat, File, Eye, CheckCircle, XCircle, Upload,
  FileText, PenLine, Table, XCircle
} from "lucide-react";
import { getActionButton } from "../utils/helpers";

const MainContent = ({
  activeView,
  setActiveView,
  showFilter,
  setShowFilter,
  showNewBillMenu,
  setShowNewBillMenu,
  showMoreOptionsMenu,
  setShowMoreOptionsMenu,
  showSettings,
  newBillMenuRef,
  moreOptionsMenuRef,
  handleNewBillOption,
  handleMoreOption,
  sampleBillsData,
  fileInputRef,
  handleFileUpload
}) => {
  return (
    <div className={`flex-1 p-6 overflow-hidden ${showSettings ? 'opacity-30' : ''}`}>
      <p className="text-sm text-gray-500">Bill Pay</p>
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-2xl font-bold">Bills</h1>
        <div className="flex items-center space-x-3">
          {/* More Options Button with Dropdown */}
          <div className="relative" ref={moreOptionsMenuRef}>
            <button
              onClick={() => setShowMoreOptionsMenu(!showMoreOptionsMenu)}
              className="p-2 border rounded-lg"
            >
              <MoreVertical size={20} />
            </button>
            {showMoreOptionsMenu && (
              <div className="absolute right-0 mt-2 w-64 bg-white border border-gray-200 rounded-lg shadow-lg z-10">
                <div className="p-2">
                  <div
                    className="flex items-center p-2 hover:bg-gray-100 cursor-pointer"
                    onClick={() => handleMoreOption("download_report")}
                  >
                    <MoreVertical size={16} className="mr-3 text-gray-600" />
                    <span>Download AP aging report</span>
                  </div>
                  <div
                    className="flex items-center p-2 hover:bg-gray-100 cursor-pointer"
                    onClick={() => handleMoreOption("settings")}
                  >
                    <MoreVertical size={16} className="mr-3 text-gray-600" />
                    <span>Settings</span>
                  </div>
                </div>
              </div>
            )}
          </div>
          <button className="px-3 py-1 border border-gray-800 bg-white text-black rounded flex items-center">
            <Repeat size={16} className="mr-2" />
            Recurring Bill
          </button>
          {/* New Bill Button with Dropdown */}
          <div className="relative" ref={newBillMenuRef}>
            <button
              onClick={() => setShowNewBillMenu(!showNewBillMenu)}
              className="px-3 py-1 bg-orange-500 text-white rounded flex items-center"
            >
              <Plus size={16} className="mr-2" />
              New Bill
            </button>
            {showNewBillMenu && (
              <div className="absolute right-0 mt-2 w-80 bg-white border border-gray-200 rounded-lg shadow-lg z-10">
                <div className="p-4">
                  <div
                    className="flex items-center mb-3 cursor-pointer"
                    onClick={() => handleNewBillOption("automatic")}
                  >
                    <FileText size={16} className="mr-2" />
                    <span className="font-medium">Automatic bill creation</span>
                  </div>
                  <div
                    className="flex items-center mb-3 cursor-pointer"
                    onClick={() => handleNewBillOption("upload")}
                  >
                    <FileText size={16} className="mr-2" />
                    <span className="font-medium">Select invoice to upload</span>
                  </div>
                  <div
                    className="mb-3 p-4 border border-dashed rounded flex flex-col items-center justify-center cursor-pointer"
                    onClick={() => handleNewBillOption("upload_document")}
                  >
                    <Upload size={20} />
                    <p className="text-center mt-2 text-sm">Upload invoice over the page or click to select</p>
                  </div>
                  <p className="text-xs text-gray-500 mb-3">Upload your PDF, PNG, or JPEG invoice</p>
                  <div
                    className="flex items-center mb-3 cursor-pointer"
                    onClick={() => handleNewBillOption("manual")}
                  >
                    <PenLine size={16} className="mr-2" />
                    <span className="font-medium">Add invoice manually</span>
                  </div>
                  <div
                    className="flex items-center cursor-pointer"
                    onClick={() => handleNewBillOption("spreadsheet")}
                  >
                    <Table size={16} className="mr-2" />
                    <span className="font-medium">Upload spreadsheet</span>
                  </div>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
      <div className="mb-6">
        <div className="flex border-b mb-4 overflow-x-auto">
          {[
            { value: "overview", label: "Overview" },
            { value: "drafts", label: "Drafts" },
            { value: "for-approval", label: "For Approval" },
            { value: "for-payment", label: "For Payment" },
            { value: "history", label: "History" }
          ].map(tab => (
            <div
              key={tab.value}
              className={`px-4 py-2 cursor-pointer whitespace-nowrap ${activeView === tab.value ? 'border-b-2 border-orange-500 text-orange-500' : 'text-gray-600'}`}
              onClick={() => setActiveView(tab.value)}
            >
              {tab.label}
            </div>
          ))}
        </div>
      </div>
      {/* Search and Action buttons */}
      <div className="flex justify-between items-center mb-4">
        <div className="relative">
          <input
            type="text"
            placeholder="Search"
            className="pl-10 pr-3 py-2 border rounded w-64"
          />
          <Search className="absolute left-3 top-2.5 h-4 w-4 text-gray-400" />
        </div>
        <div className="flex items-center space-x-2">
          <button
            className="p-2 border rounded-lg hover:bg-gray-50"
            onClick={() => setShowFilter(!showFilter)}
          >
            <Filter size={20} />
          </button>
          <button className="p-2 border rounded-lg hover:bg-gray-50">
            <Calendar size={20} />
          </button>
          <button className="p-2 border rounded-lg hover:bg-gray-50">
            <Columns size={20} />
          </button>
          <button className="p-2 border rounded-lg hover:bg-gray-50">
            <Download size={20} />
          </button>
        </div>
      </div>
      {showFilter && (
        <div className="flex items-center mb-4 p-2 bg-gray-50 rounded-lg">
          <button className="px-3 py-1 bg-blue-600 text-white rounded mr-2 flex items-center">
            <Plus size={16} className="mr-2" />
            Add Filter
          </button>
          <button className="ml-auto" onClick={() => setShowFilter(false)}>
            <XCircle className="h-4 w-4" />
          </button>
        </div>
      )}
      {/* Updated Bills Table with all the requested columns */}
      <div className="overflow-x-auto border rounded-lg">
        <table className="w-full border-collapse">
          <thead className="bg-gray-50">
            <tr className="border-b">
              <th className="w-10 text-left py-3 px-2 sticky left-0 bg-gray-50">
                <input type="checkbox" className="h-4 w-4" />
              </th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Vendor</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Status</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Amount</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Invoice Date</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Vendor Owner</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Invoice Copy</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Payment Date</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Due Date</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Payment Status</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Payment Method</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Approval Status</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Category</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Invoice Number</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">PO Number</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Recurring</th>
              <th className="text-left py-3 px-3 whitespace-nowrap">Action</th>
            </tr>
          </thead>
          <tbody>
            {sampleBillsData.map((bill) => (
              <tr key={bill.id} className="border-b hover:bg-gray-50">
                <td className="py-3 px-2 sticky left-0 bg-inherit">
                  <input type="checkbox" className="h-4 w-4" />
                </td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.vendor}</td>
                <td className="py-3 px-3 whitespace-nowrap">
                  <span className={`px-2 py-1 rounded-full text-xs ${
                    bill.status === "Draft" ? "bg-gray-200" :
                    bill.status === "For Approval" ? "bg-yellow-100 text-yellow-800" :
                    "bg-blue-100 text-blue-800"
                  }`}>
                    {bill.status}
                  </span>
                </td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.amount}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.invoiceDate}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.vendorOwner}</td>
                <td className="py-3 px-3 whitespace-nowrap">
                  <button className="text-blue-500 hover:underline flex items-center">
                    <File size={14} className="mr-1" />
                    View
                  </button>
                </td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.paymentDate}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.dueDate}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.paymentStatus}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.paymentMethod}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.approvalStatus}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.category}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.invoiceNumber}</td>
                <td className="py-3 px-3 whitespace-nowrap">{bill.poNumber}</td>
                <td className="py-3 px-3 whitespace-nowrap">
                  {bill.isRecurring ? (
                    <span className="flex items-center text-blue-600">
                      <Repeat size={14} className="mr-1" />
                      Yes
                    </span>
                  ) : "No"}
                </td>
                <td className="py-3 px-3 whitespace-nowrap">{getActionButton(bill)}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <input
        type="file"
        accept="image/*,application/pdf"
        ref={fileInputRef}
        onChange={handleFileUpload}
        className="hidden"
      />
    </div>
  );
};

export default MainContent;