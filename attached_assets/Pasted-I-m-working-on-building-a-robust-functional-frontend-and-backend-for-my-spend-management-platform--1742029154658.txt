I’m working on building a robust, functional frontend and backend for my spend management platform, Stride, a B2B SaaS platform for the Indian market, similar to Ramp and Brex. The backend is built with Python, Flask, Flask-SQLAlchemy, and PostgreSQL, served with Gunicorn, and the frontend uses React with Vite. The app is running on Replit, and the sidebar includes tabs like Inbox, Insights, Expense, Accounting, Bill Pay (Bills, Purchase Order, Vendors), Card, Policy, and People. I’ve enabled the Google Document AI API under a single service account for "My First Project," with assigned roles: Document AI API User and Document AI Viewer. I’ve provided the service account JSON credentials to Replit as GOOGLE_APPLICATION_CREDENTIALS_JSON. The current utils/ocr_vision.py file (extracted content provided) implements Document AI OCR and NLP processing for invoices, receipts, purchase orders (POs), goods receipt notes (GRNs), and expense policies, with cost optimization (default OCR, selective NLP via use_nlp), multi-tenancy support (tenant_id), and usage tracking. However, the file is overly long and mixes multiple concerns (client setup, OCR, NLP, field extraction, usage tracking), making it hard to scale, debug, and maintain. I need to modularize it into smaller files within a new utils/document_ai/ directory, following best industry practices for single responsibility, scalability, and maintainability. The extracted content shows OCR artifacts (e.g., Page 3-7 “nlp991”, Page 30 “logger bs”), which should be corrected based on context. This is Step 20.2 of our development process.

Task: Modularize the existing utils/ocr_vision.py file into smaller, focused files within a new utils/document_ai/ directory, ensuring each file has a single responsibility, is under 200-300 lines, and preserves functionality (OCR, NLP, multi-tenancy, cost tracking).

Create Directory and Initial Setup
Task: Set up the utils/document_ai/ directory and move relevant imports.
Instruct Replit to create a new directory utils/document_ai/ and copy the existing utils/ocr_vision.py as a backup (e.g., utils/ocr_vision_backup.py).
Guide Replit to add common imports (os, io, json, base64, logging, re, enum, typing) to a new utils/document_ai/__init__.py file with a docstring: "Initialize the Document AI module for Stride.".
Direct Replit to ensure google-cloud-documentai is in requirements.txt.