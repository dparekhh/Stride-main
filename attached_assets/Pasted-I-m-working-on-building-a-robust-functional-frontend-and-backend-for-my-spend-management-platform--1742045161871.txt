I’m working on building a robust, functional frontend and backend for my spend management platform, Stride, a B2B SaaS platform for the Indian market, similar to Ramp and Brex. The backend is built with Python, Flask, Flask-SQLAlchemy, and PostgreSQL, served with Gunicorn, and the frontend uses React with Vite. The app is running on Replit, and the sidebar includes tabs like Inbox, Insights, Expense, Accounting, Bill Pay (Bills, Purchase Order, Vendors), Card, Policy, and People. I’ve enabled the Google Document AI API under a single service account for "My First Project," with assigned roles: Document AI API User and Document AI Viewer. I’ve provided the service account JSON credentials to Replit as GOOGLE_APPLICATION_CREDENTIALS_JSON. The utils/ocr_vision.py file implements Document AI OCR and NLP processing for invoices, with multi-tenancy support (tenant_id) and usage tracking.

I’ve attached two screenshots:

Image 1: The current "New Bill" page built by Replit, showing the "Invoice Preview" on the left and the "Who's it for?" section on the right. The right side correctly shows the message "We couldn't find an existing vendor that matches this bill..." and a "Select another vendor" search bar, but it’s missing the "Create new vendor from invoice" section with the extracted vendor name (e.g., "Jefferson Healthcare") and the "Create new vendor" button.
Image 2: The desired "New Bill" page, showing the same "Invoice Preview" and "Who's it for?" section, but with the "Create new vendor from invoice" heading, the dynamically extracted vendor name ("Sassy SAAS Vendor, Inc."), and a "Create new vendor" button below it.
The issue is that Replit is unable to create the "Create new vendor from invoice" part, including printing the vendor name extracted via OCR (e.g., "Jefferson Healthcare" or "Sassy SAAS Vendor, Inc.") below that heading and adding the "Create new vendor" button. The backend is already set up to extract the vendor name using process_document_image from utils/ocr_vision.py and check if the vendor exists in the database, passing vendor_name and vendor_exists to the frontend. However, the frontend React component is not rendering the expected section when vendor_exists is false.

Task: Update the React frontend to add the "Create new vendor from invoice" section with the extracted vendor name and "Create new vendor" button when the vendor is not found, matching Image 2.