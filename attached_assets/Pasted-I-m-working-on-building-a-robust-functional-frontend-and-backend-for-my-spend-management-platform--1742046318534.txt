I’m working on building a robust, functional frontend and backend for my spend management platform, Stride, a B2B SaaS platform for the Indian market, similar to Ramp and Brex. The backend is built with Python, Flask, Flask-SQLAlchemy, and PostgreSQL, served with Gunicorn, and the frontend uses React with Vite. The app is running on Replit, and the sidebar includes tabs like Inbox, Insights, Expense, Accounting, Bill Pay (Bills, Purchase Order, Vendors), Card, Policy, and People. I’ve enabled the Google Document AI API under a single service account for "My First Project," with assigned roles: Document AI API User and Document AI Viewer. I’ve provided the service account JSON credentials to Replit as GOOGLE_APPLICATION_CREDENTIALS_JSON. The utils/ocr_vision.py file implements Document AI OCR and NLP processing for invoices, with multi-tenancy support (tenant_id) and usage tracking.

I’ve attached two screenshots:

Image 1: The current "New Bill" page built by Replit, showing the "Invoice Preview" on the left and the "Who's it for?" section on the right. The right side shows the warning message "We couldn't find an existing vendor that matches this bill..." but is missing the "Create new vendor from invoice" section with the extracted vendor name (e.g., "Jefferson Healthcare") and the "Create new vendor" button.
Image 2: The desired "New Bill" page, showing the same layout but with the "Create new vendor from invoice" heading, the dynamically extracted vendor name ("Sassy SAAS Vendor, Inc."), and a "Create new vendor" button below it.
Replit’s response to Step 2 indicates that the "Who's it for?" section, including the "Create new vendor from invoice" logic, is in frontend/src/components/InvoicePreview.jsx. The section is present in the code but may not be rendering due to issues with extractedData.vendor_name being undefined or a logic error. The backend is set up to extract the vendor name using process_document_image from utils/ocr_vision.py and pass vendor_name and vendor_exists to the frontend, but the frontend isn’t displaying the expected section when vendor_exists is false.

Task: Debug and fix the "Create new vendor from invoice" section in InvoicePreview.jsx to ensure it renders with the extracted vendor name and "Create new vendor" button when vendor_exists is false, matching Image 2.

Please break this into small baby steps for seamless execution.

Step 1: Verify the Backend Data Passing to InvoicePreview.jsx
Task: Confirm that the backend is correctly passing vendor_name and vendor_exists to the frontend.

Instruct Replit to check the Flask route handling the "New Bill" page (likely in app/routes/bill_routes.py or a similar file).
Guide Replit to ensure the route calls process_document_image from utils/ocr_vision.py and extracts the vendor_name (e.g., "Jefferson Healthcare" from Image 1 or "Sassy SAAS Vendor, Inc." from Image 2) using extract_invoice_fields.
Direct Replit to:
Log the extracted vendor_name (e.g., logging.info(f"Extracted vendor name: {vendor_name}")).
Pass vendor_name and vendor_exists to the frontend via the template or JSON response (e.g., {"vendor_name": "Jefferson Healthcare", "vendor_exists": false}).
Ensure extractedData in InvoicePreview.jsx is populated with vendor_name (e.g., check if props.extractedData.vendor_name or a state variable contains the value).
Deliverables: Share the Flask route code, the log output with the extracted vendor_name, and confirm the data is received in InvoicePreview.jsx (e.g., console.log the props or state).