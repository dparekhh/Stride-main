Implement a centralized transaction data management system for the Stride Card transactions that follows our established patterns. The implementation should:

1. Create a new data store in frontend/src/utils/strideCardData.js to centralize all transaction-related data and utilities:
- Move the transaction data from Accounting_StrideCardPage.jsx
- Add helper functions for data transformation and filtering
- Structure the code to easily switch between mock data and API data later
- Follow the pattern seen in frontend/src/utils/data.js and frontend/src/utils/accountingData.js

2. Update the AccountingContext in frontend/src/contexts/AccountingContext.jsx to handle:
- Transaction data state management
- Loading states for transactions
- Error handling
- Data fetching and caching logic
- Pagination if needed
- Methods for transaction data updates

3. Modify Accounting_StrideCardPage.jsx to:
- Remove the hardcoded transaction data
- Use AccountingContext for data access
- Add loading states
- Add error handling
- Keep the existing UI and filtering logic

The implementation should follow these patterns from our codebase:
- Use the same state management pattern as seen in frontend/src/contexts/AccountingContext.jsx
- Follow the data organization pattern from frontend/src/utils/accountingData.js
- Maintain consistency with our existing transaction handling in frontend/src/utils/data.js
- Keep the same UI patterns used in CardTransactionExpenses.jsx

The data structure should support:
- Easy API integration in the future
- Efficient filtering and sorting
- Transaction status tracking
- Accounting category mapping
- Receipt status tracking
- Policy compliance checks

Requirements:
- Keep existing UI functionality intact
- Support all current filtering capabilities
- Maintain existing column management
- Keep the current checkbox selection system
- Support the export functionality
- Allow for future API pagination
- Handle loading and error states consistently
- Support offline data caching

Do not modify:
- The existing UI components
- The column management logic
- The filter implementation
- The checkbox selection system