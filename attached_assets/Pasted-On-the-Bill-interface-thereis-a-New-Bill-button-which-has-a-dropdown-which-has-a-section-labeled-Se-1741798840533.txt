On the Bill interface thereis a New Bill button which has a dropdown which has a section labeled "Select invoice to upload," (Bill Pay -> Bill -> New Bill Button -> Select invoice to upload) and within that section, there is a placeholder area with the text "Upload invoice over the page or click to select." This area is currently static, and I want to make it interactive so users can either click on it or drag and drop a file to upload an invoice. The supported file types for the upload should be PDF, JPG, and PNG.

When a user clicks on the placeholder area or drags and drops a file, the application should allow them to select a file from their computer. After selecting a file, the application should validate that the file is of the correct type (PDF, JPG, or PNG). If the file type is incorrect, display an error message to the user, such as "Invalid file type. Please upload a PDF, JPG, or PNG file." If the file is valid, the application should display a preview of the file name (and, if possible, a thumbnail for image files like JPG or PNG) below the upload area. 

To prepare for future updates, the uploaded file should be stored in a way that it can be easily accessed and passed to the next page in the workflow (for example, as part of a form submission or through session storage). After a successful upload, the application should automatically redirect the user to a new page (for now, this can be a placeholder page with a simple message like "Starting Bill Pay flow..."), where future functionality will be added, such as creating a vendor and submitting a bill. Ensure that the file upload logic is modular and well-documented, so it can be seamlessly integrated with future features like database interactions (e.g., checking for existing vendors) and form submissions on the next page.

Additionally, ensure the upload area has visual feedback: when a user hovers over the area, it should change its border color or background to indicate it’s interactive, and when a file is being dragged over the area, it should display a "Drop here" message or similar visual cue. The design should remain consistent with the existing UI, which uses a clean, modern style with a white background, subtle shadows, and orange accents. Please implement this functionality using HTML, CSS, and JavaScript, ensuring it works smoothly on Replit’s environment, and structure the code to support future extensions without requiring significant refactoring.