Please implement the following enhancements to the ConnectHRISDrawer component:
1. HRIS Provider List Organization:
- Sort HRIS providers alphabetically by name
- Add connection status tracking for each provider
- Implement visual indicators for connected providers (green blinking dot)
- Move connected providers to the top of the list
2. Search Functionality:
- Add a clean, minimalist search bar below the column headers
- Search bar should span the full width of the drawer
- Use consistent styling with existing components
- Add search icon on the left side of the input
- Implement real-time filtering of HRIS providers
3. UI Components:
- Column Headers:
  * Left: "Name of HRIS"
  * Right: "Sync"
- Search Bar:
  * Full-width design below headers
  * Light gray border (border-gray-200)
  * Subtle shadow
  * Placeholder text: "Search HRIS providers..."
  * Search icon from lucide-react
  * Consistent padding with other elements
4. Connection Status Features:
- Add green blinking dot next to connected HRIS names:
  * Use keyframe animation for blinking effect
  * Size: 8px diameter
  * Color: green-500
- Transform "Sync" button to "Invite people" when connected:
  * Change button color to primary theme color
  * Update hover states accordingly
5. Button States:
- Sync Button:
  * Default state: Gray border with subtle shadow
  * Connected state: Primary color with "Invite people" text
- Done Button:
  * Use primary theme color
  * Consistent hover effect

6. Styling Requirements:
- Maintain consistent spacing (p-6)
- Use existing color scheme
- Implement smooth transitions
- Ensure proper mobile responsiveness
- Follow existing shadow patterns
7. Implementation Details:
- Use React useState for search and connection states
- Implement proper TypeScript types if applicable
- Follow existing error handling patterns
- Use existing NotificationContext for success/error messages
- Implement proper cleanup in useEffect if needed
The implementation should follow the existing drawer patterns in the codebase and maintain consistent styling with the rest of the application. Use the AppDrawer component as the foundation and ensure proper state management for search functionality and connection status.

The UI should closely match the clean, minimal design shown in image_1743389294873.png, with particular attention to the search bar styling and provider list organization.