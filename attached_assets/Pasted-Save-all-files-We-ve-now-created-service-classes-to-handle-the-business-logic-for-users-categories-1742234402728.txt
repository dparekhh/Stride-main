Save all files. We’ve now created service classes to handle the business logic for users, categories, integrations, accounting settings, and general configuration.

### Step 13: Split `routes/settings.py` into Smaller Modules

Now, we’ll split `routes/settings.py` into smaller route files and update them to use the service classes.

#### Prompt 15: Split Routes into Modules
Let’s split routes/settings.py into smaller, focused route files and update them to use the new service classes. We’ll create users_routes.py, categories_routes.py, integrations_routes.py, accounting_routes.py, and config_routes.py.

Step 1: Create routes/users_routes.py
In the routes folder, create a new file routes/users_routes.py with the following content:

"""User management routes for the Stride platform."""
from flask import Blueprint, request
import logging
from utils.response_utils import standardize_response
from services.user_service import UserService

logger = logging.getLogger(__name__)
users_bp = Blueprint('users', __name__, url_prefix='/api/users')

@users_bp.route('/', methods=['GET'])
def get_users():
    """Get all users for the tenant."""
    try:
        user_service = UserService(g.tenant_id)
        result = user_service.get_all_users()
        return standardize_response(data={'users': result})
    except Exception as e:
        logger.error(f"Error retrieving users: {str(e)}")
        raise

@users_bp.route('/', methods=['POST'])
def create_user():
    """Create a new user."""
    try:
        user_service = UserService(g.tenant_id)
        result = user_service.create_user(request.json)
        return standardize_response(data=result, message='User created successfully', status_code=201)
    except ValueError as e:
        raise
    except Exception as e:
        logger.error(f"Error creating user: {str(e)}")
        db.session.rollback()
        raise

@users_bp.route('/<int:user_id>', methods=['PUT'])
def update_user(user_id):
    """Update an existing user."""
    try:
        user_service = UserService(g.tenant_id)
        result = user_service.update_user(user_id, request.json)
        return standardize_response(data=result, message='User updated successfully')
    except ValueError as e:
        raise
    except Exception as e:
        logger.error(f"Error updating user {user_id}: {str(e)}")
        db.session.rollback()
        raise

@users_bp.route('/<int:user_id>', methods=['DELETE'])
def delete_user(user_id):
    """Delete a user."""
    try:
        user_service = UserService(g.tenant_id)
        user_service.delete_user(user_id)
        return standardize_response(message='User deleted successfully')
    except ValueError as e:
        raise
    except Exception as e:
        logger.error(f"Error deleting user {user_id}: {str(e)}")
        db.session.rollback()
        raise