The changes and instructions dont seem to be implemented. When i am clicking on the edit button on Review Exisiting Vendor Page,  i am being taken to NEw Bill page and not the Vendor Information page. 

Verify the handleEdit Logic in VendorReview.jsx
The handleEdit function should explicitly navigate to /vendor-information when the vendor is existing (sourceRoute === "newBill"). Here’s the corrected version of VendorReview.jsx:
// src/components/VendorReview.jsx
import React from "react";
import { ArrowLeft, Check, User, Mail, Phone, MapPin, Building2, FileText, Edit2 } from "lucide-react";
import SharedInvoicePreview from "./SharedInvoicePreview";
import { useNavigate } from "react-router-dom";

const VendorReview = ({ 
  formData,
  uploadedInvoice,
  onBack,
  onContinue,
  isSubmitting,
  sourceRoute = "newVendor"
}) => {
  const navigate = useNavigate();

  const buttonLabel = sourceRoute === "newBill" ? "Continue" : "Create vendor";
  const isExistingVendor = sourceRoute === "newBill";

  // Handle edit button click
  const handleEdit = () => {
    console.log("Edit button clicked, sourceRoute:", sourceRoute); // Debug log
    if (isExistingVendor) {
      navigate("/vendor-information", { state: { formData, uploadedInvoice, sourceRoute } });
    } else {
      onBack(); // For new vendor, go back to NewVendor page
    }
  };

  // ... (rest of the component remains unchanged until the footer)

  return (
    <div className="fixed inset-0 z-50 flex bg-white">
      {/* Left side - Invoice Preview */}
      <SharedInvoicePreview uploadedInvoice={uploadedInvoice} />
      
      {/* Right side - Vendor Review */}
      <div className="w-1/2 flex flex-col">
        {/* Header */}
        <div className="p-4 border-b border-gray-200 flex items-center justify-between">
          <div className="flex items-center">
            <button 
              className="mr-2 text-gray-600 hover:text-gray-900"
              onClick={onBack}
            >
              <ArrowLeft size={20} />
            </button>
            <h2 className="text-xl font-semibold text-gray-800">
              {isExistingVendor ? "Review Existing Vendor" : "Review New Vendor"}
            </h2>
          </div>
        </div>
        
        {/* Review content */}
        <div className="flex-grow overflow-y-auto p-6 bg-gray-50">
          {/* ... (vendor owner, contact, payment, tax sections unchanged) */}
        </div>
        
        {/* Footer with buttons */}
        <div className="p-4 border-t border-gray-200 flex justify-between items-center">
          <button
            className="flex items-center px-3 py-1.5 text-sm text-gray-700 bg-white border border-gray-300 rounded hover:bg-gray-50"
            onClick={handleEdit} // Ensure this uses handleEdit
          >
            <Edit2 size={16} className="mr-1.5" />
            Edit
          </button>
          
          <button 
            className={`px-4 py-2 ${isSubmitting ? "bg-orange-500" : "bg-orange-500 hover:bg-orange-600"} text-white font-medium rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-400 flex items-center`}
            onClick={onContinue}
            disabled={isSubmitting}
          >
            {isSubmitting ? (
              <>
                <svg className="animate-spin -ml-1 mr-2 h-4 w-4 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
                Processing...
              </>
            ) : (
              buttonLabel
            )}
          </button>
        </div>
      </div>
    </div>
  );
};

export default VendorReview;

export default VendorReview;

Changes:

Added a console.log to debug the handleEdit function and confirm it’s being triggered.
Ensured the "Edit" button uses onClick={handleEdit} (not onBack).
Double-check that the navigation is explicitly set to /vendor-information.